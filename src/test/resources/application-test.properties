# Database
spring.datasource.url=jdbc:postgresql://localhost:5433/furcode_test
spring.datasource.username=postgres
spring.datasource.password=test_password

# MinIO
minio.endpoint=http://localhost:9090
minio.accessKey=test_root
minio.secretKey=test_roottoor
minio.bucket=furcodetest

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6380

# Logging
logging.level.root=WARN
logging.level.org.springframework=WARN
logging.level.org.hibernate=WARN
logging.level.org.mindera.fur.code=INFO

# Disable unnecessary auto-configuration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Disable OpenAI for tests
spring.ai.openai.api-key=test-key
spring.ai.openai.enabled=falseions.model=gpt-3.5-turbo